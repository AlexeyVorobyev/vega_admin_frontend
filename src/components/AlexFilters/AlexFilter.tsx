import {FC} from "react";
import {IAlexFilter} from "./alexFiltersMap";

interface IProps {
    config: IAlexFilter
}

export const AlexFilter: FC<IProps> = ({
                                    config
                                }) => {

    return null
}